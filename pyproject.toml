[project]
name = "hw06"
version = "0.1.0"
description = "Spark benchmarks for DataFrame/RDD/SQL API comparison"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psutil>=7.2.2",
    "pyspark==4.1.1",
]
